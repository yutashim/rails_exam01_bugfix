<h1>Editing Blog</h1>

<%# ErrorCause: can't render form, use `%=` %>
<%# FixReason: use `%=` to render form %>
<%= render 'form', blog: @blog %>

<%= link_to 'Show', @blog %> |
<%= link_to 'Back', blogs_path %>
