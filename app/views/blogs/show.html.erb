<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @blog.content %>
</p>

<% unless @blog.comments.count.zero? %>
  <hr>

  <p>
    <strong>Comment:</strong>
    <%# ErrorCause: TypeCastError: String can't be coerced into Integer %>
    <%# FixReason: use to_s to convert Integer to String %>
    <%= "#{@blog.comments.count} comments" %>
  </p>

  <p>
    <%= render @blog.comments %>
  </p>
<% end %>

<p>
  <%= render 'comments/form', blog: @blog %>
</p>
<%# ErrorCause: can't render blog_edit_path %>
<%# FixReason: use double quote to string interpolation %>
<%# ErrorCause: undefined local variable or method `blog' %>
<%# FixReason: set instance variable @blog %>
<%= link_to 'Edit', "/blogs/#{@blog.id}/edit" %> |
<%= link_to 'Back', blogs_path %>
